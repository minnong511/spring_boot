# 싱글톤
- 싱글톤 : 객체를 딱 1개만 만들어서 함께 사용
예시 : @Service, @Controller, @Repository -> 기본적으로 싱글톤 


@Service
public class UserService {
}

-> 이거는 사실 

UserService userService = new UserService(...); 

-> 객체를 하나만 만들어서 쓴다 

이렇게 쓰는 이유

Service나 Repository는 보통 “기능을 수행하는 도구”라서, 요청마다 새로 만들 필요가 없기 때문에 
객체를 하나만 만들면 된다. 

다만 싱글톤 Service 안에 요청별 데이터처럼 이런 필드를 두면 좋지 않다. 

@Service
public class UserService {
    private String currentUserName; // 위험할 수 있음
}

-> UserService 객체를 하나만 만든다. -> currentUserName은 UserService 객체가 살아 있는 동안 계속 유지되는 필드(공용 변수)
-> 동시성 문제 발생 가능 

안전한 방식은 필요값을 메서드의 매개변수나 지역변수로 두는 것 

@Service
public class UserService {

    public String getWelcomeMessage(String userName) {
        String message = userName + "님, 환영합니다.";
        return message;
    }
}





# 멀티톤 
- 멀티톤 : 종류별로 정해진 객체를 여러 개 만들어 정리  


-> 키마다 하나씩 객체를 보관하는 방식 

Map<String, PaymentService> services = new HashMap<>();

services.put("KOREA", new PaymentService("원화"));
services.put("USA", new PaymentService("달러"));

"KOREA" → PaymentService 1개
"USA"   → PaymentService 1개

멀티톤(Multiton)은 "객체를 무한히 새롭게 만드는 것이 아니라, 정해진 키마다 객체를 하나씩 만들어 공유하는 방식"

싱글톤은 전체에 하나 

UserService 객체: 1개

멀티톤은 종류별로 하나 

KOREA 결제 서비스: 1개
USA 결제 서비스: 1개
JAPAN 결제 서비스: 1개

import java.util.HashMap;
import java.util.Map;

public class PaymentServiceManager {

    private static final Map<String, PaymentService> services = new HashMap<>();

    public static PaymentService getService(String country) {
        if (!services.containsKey(country)) {
            if (country.equals("KOREA")) {
                services.put(country, new PaymentService("KRW"));
            } else if (country.equals("USA")) {
                services.put(country, new PaymentService("USD"));
            }
        }

        return services.get(country);
    }
}