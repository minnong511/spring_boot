# 현재 프로젝트 디렉토리 

--- 

HTTP 요청 
-> Controller 
-> Service 
-> Repository 
-> JPA/Hibernate 
-> H2 DB
-> JSON 응답 

# 1. 애플리케이션 시작 

GET /api/users

UserController.getAllusers()
  ↓
UserService.getAllusers()
  ↓
UserRepository.findAll()
  ↓
SELECT * FROM users
  ↓
List<User>를 JSON으로 반환

# 2. 사용자 CRUD 로직

GET /api/users 

실행 흐름: 

UserContorller.getAllusers() 
-> 
UserService.getAllusers()
-> 
UserRepository.findAll()
->
SELECT * From users 
->
List<User>를 JSON으로 반환 

findAll() -> 선언하지 않아도 -> JpaRepository<User, Long> 을 상속해서 
Spring Data JPA가 구현체를 자동으로 생성 

# 3. 특정 사용자 조회 

GET /api/users/1 

plain text 

URL의 1 
-> @pathVariable 
Controller 
-> 
userRepository.findById(1)
->
Optional<User> 

사용자가 없으면 Optional.empty()가 반환 

# 4. 사용자 생성 

POST /api/users
Content-Type: application/json

{
  "name": "이민형",
  "email": "min@example.com"
}

실행 흐름 : 

JSON 
-> 
UserRequest 
-> @Valid 유효성 검사 
User 객체 생성 
-> 
userRepository.save(user)
->
INSERT INTO users 
-> 
201 Created + 저장된 User

# 5. 사용자 수정 

findById(id)
-> 
사용지가 있으면 name, email 
-> 
save(user)

하지만 UserController에 @PutMapping이 없어서 현재 HTTP 요청으로는 호출할 수 없다. 

# 6. Entity와 H2 DB

User.java -> DB의 users 테이블과 연결된다 .

User.id -> users.id 
User.name -> users.name 
User.email -> users.email 

애플리케이션이라서 
1. hibernate가 users 테이블을 새로 생성 
2. data.sql이 실행 
3. 홍길동, 김철수, 이영희 데이터가 들어간다. 

메모리 DB이기 때문에 애플리케이션을 종료하면 데이터가 사라진다. 

# UserService 객체 생성 방식 

UserController
  ↓
UserService 프록시
  ↓ 실행 시작 로그
실제 UserService
  ↓
Repository
  ↓ 실행 종료 및 소요 시간 로그

따라서 Controller가 주입받는 것은 단순 UserService 객체가 아니라 실행시간 측정 기능이 추가된 프록시 객체

5. 비동기 로직 

스레드 풀 설정 

스레드 풀 설정:
기본 스레드 2개
최대 5개
대기 큐 100개
스레드 이름 async-...


# --------- -

질문 

UserController 객체는 누가 생성하나요?
UserServiceConfig의 @Bean 메서드가 객체를 생성

UserService 객체는 누가, 어디에서 생성하나요?
@RequiredArgsConstructor 이라는 lombok의 라이브러리가 생성합니다. 

userService 필드에는 어떻게 객체가 들어가나요?
public user (UserService userService){
    this.userService = userService 
}
잘 모르겠다. 

이 과정에서 IoC와 DI는 각각 무엇을 의미하나요?
Inversion of control 
Dependency Injection 

Spring boot가 annotation을 읽고 객체를 생성하고, 의존성을 관리하는 것을 의미합니다. 사용자가 직접 지정하거나 생성할 필요없습니다. 

@RequiredArgsConstructor가 UserService 객체까지 생성해 주는 것인가요?

그것은 아닙니다. userService는 미리 정의를 해놓고 임포트 해야합니다.

# 1. UserController 객체는 누가 생성하나요? 

UserServiceConfig의 @Bean 메서드가 객체를 생성 -> 이거는 UserService 객체를 만드는 거지 UserController 객체를 만드는 것이 아니다

UserController에는 @RestController가 붙어있다. 

@RestController
public class UserController {
}

Spring Boot가 컴포넌트 스캔을 하면서 @RestController를 발견하고 UserController 객체를 생성합니다. 

정확한 표현 
- UserController 객체는 Spring 컨테이너가 생성한다. Spring Boot가 컴포넌트 스캔 중 @RestController 가 붙은 클래스를 발견하여 객체를 생성하고 Bean으로 등록한다. 

# 2.UserService 객체는 누가, 어디에서 생성? 

@RequiredArgsConstructor 는 UserService 객체를 생성하지 않는다. Lombok이 만들어주는 것은 객체가 아니라 생성자 코드 

@Bean
public UserService userService(UserRepository userRepository) {
    UserService userService = new UserService(userRepository);
    // ...
    return (UserService) proxyFactory.getProxy();
}

이게 객체 생성 코드 

- 개발자가 new UserService(userRepository) 코드를 작성
- Spring이 @Bean 메서드를 호출
- @Bean 메서드가 UserService 대상 객체를 만듦
- 프록시로 감싸서 Spring 컨테이너에 등록

Spring이 UserServiceConfig의 @Bean 메서드를 호출하고, 그 메서드 안의 new UserService(userRepository)가 실제 UserService 객체를 생성한다. 최종적으로는 프록시 객체가 Bean으로 등록된다. 

컴파일할 때 Lombok이 다음 생성자를 만들어준다. 

public UserController(UserService userService) {
    this.userService = userService;
}

이후 Spring이 컨테이너에서 UserService 타입의 Bean을 찾아 생성자 인수로 전달 

Spring 컨테이너에서 UserService Bean 탐색 
-> UserController 생성자에 전달 
-> this.userService에 저장

이것이 생성자 주입 방식의 DI 

# 3. IoC 와 DI는? 

Inversion of control
Dependency Injection

작성한 설명: 

Spring boot가 annotation을 읽고 객체를 생성하고, 의존성을 관리하는 것을 의미합니다. -> 대표적인 오답 

IoC(Inversion of Control)

원래 인간이 개발자가 객체를 생성 -> Spring boot -> Spring 컨테이너가 객체를 생성하고 생명주기 관리, 연결도 알아서 해준다. 

DI 

DI는 Dependency Injection : 의존성 주입 

UserController는 기능 수행을 하려면 UserService를 필요함 
-> 생성하지는 않았어

어떤 객체가 필요로 하는 다른 객체를 외부에서 넣어주는 것이 DI이다. 현재 프로젝트에서는 Spring이 UserService Bean을 UserController 생성자에 전달한다.


IoC
└── 객체 생성과 관리의 제어권을 Spring이 가짐
    └── DI
        └── 필요한 객체를 생성자 등을 통해 주입

# 5. 

어떤 클래스가 Controller인지: @RestController
어떤 클래스가 설정 클래스인지: @Configuration
어떤 객체를 Bean으로 등록할지: @Bean
어떤 의존성이 필요한지: private final UserService userService
UserService를 어떻게 만들지: new UserService(userRepository)

> 개발자는 객체의 역할과 의존 관계를 선언하고, Spring 컨테이너가 그 선언을 바탕으로 객체 생성, 관리, 주입을 수행한다. 

import는 객체를 가져오는 기능이 아니다. 다른 패캐지에 있는 클래스 이름을 소스 코드에서 사용하기 위한 문법 

모범 답안 

애플리케이션이 실행되면 Spring Boot가 컴포넌트 스캔을 수행

Spring은 @RestController가 붙은 UserController를 발견
UserController를 생성하려면 생성자 인수인 UserService가 필요하다.

UserService는 @Service로 등록되는 것이 아니라, Spring이 UserServiceConfig의
@Bean 메서드를 호출해서 생성한다. 
@Bean 메서드에서는 실제 UserService를 만든 뒤 프록시로 감싸서 Bean으로 등록한다.

@RequiredArgsConstructor는 UserController의 final 필드를 인수로 받는 생성자 코드를 만들어줄 뿐, UserService 객체를 직접 생성하지 않는다. 

Spring은 등록된 UserService Bean을 UserController 생성자에 전달 
생성자는 전달받은 객체를 this.userService 필드에 저장

객체 생성과 관리의 제어권을 Spring이 갖는 것이 IoC
필요한 UserService 객체를 UserController에 넣어주는 것이 DI

# 2번 문제 : Controller -> DB 흐름 

GET /api/users1 

1. public Optional<User> getUserById(@PathVariable long  id){
        return userService.getUserById(id); 
    } 와 연결되어 user 1의 정보를 가져오게 됩니다. 

2. @Pathvariable 을 통해서 들어가게 됩니다. 
3. Controller의 경우 api 요청을 받고 service로 요청을 보냅니다., service는 실제로 로직으로 처리합니다.처리된 결과는 repository로 전송되어 DB를 조회하게 됩니다.
4. JPA에 있는 메서드입니다. 유저가 구현하지 않았고, 이미 구현되어 있습니다. 
5. Optional<User> 조회결과 없는 결과를 대비해서 Optional로 선언했습니다.
6. 결과를 반환하지 않습니다.

정답 

1. URL 경로의 1을 사용자 ID로 사용하여, ID가 1인 사용자를 조회합니다. 
2. Spring이 URL의 {id} 자리에 들어온 1을 @PathVariable이 붙은 long id 매개변수에 넣어줍니다.
3.1 Controller는 HTTP 요청을 받고 URL의 ID를 추출한 다음, UserService.getUserById(id)를 호출한다.
3.2 Service가 Repository 메서드 호출 -> Repository가 DB 조회 -> DB 조회 결과가 Repository에서 Service로 반환 -> Service가 Controller로 반환 


정확한 전체 

HTTP 요청 
-> Controller가 요청과 ID를 받음
-> Service 메서드 호출 
-> Service가 Repository 메서드 호출 
-> Repository가 DB 조회 
-> 조회 결과를 Service에 반환 
-> Service가 Controller에 반환
-> Spring이 HTTP 응답으로 반환 

HTTP 요청 
-> Controller가 요청과 ID를 받음
-> Service 메서드 호출 
-> Service가 Repository 메서드 호출 
-> Repository가 DB 조회 
-> 조회 결과를 Service에 반환 
-> Service가 Controller에 반환 
-> Spring이 HTTP 응답으로 반환 


findById()는 UserRepository가 상속한 Spring Data Repository 인터페이스에 선언된 메서드이다. 개발자는 UserRepository 구현 클래스를 작성하지 않았으며. Spring Data JPA가 실행 시점에 구현체를 생성하고 Hibernate를 통해 DB 조회한다. 

Spring Data Repository
→ findById() 같은 Repository 메서드 제공

Spring Data JPA
→ UserRepository 구현체 생성

Hibernate
→ Entity 정보를 이용해 SQL 실행

H2
→ 실제 데이터 저장 및 조회

5. Optional<User>를 사용하는 이유

조금 더 정확하게 말하자면

ID에 해당하는 사용자가 존재할 수도 있고, 존재하지 않을 수도 있기 때문에 Optional<User>를 반환한다. 

Optional.of(user) // 사용자가 있음
Optional.empty()  // 사용자가 없음
Optional은 “조회 결과가 반드시 존재한다고 가정하지 말고, 없음도 처리하라”는 의미를 타입으로 나타냅니다.

ID가 1인 사용자가 없으면 Optional.empty()를 반환한다.

다듬은 모범답안 

GET /api/users/1 요청은 @RequestMapping("/api") 와 @GetMapping("/users/{id}") 가 합쳐져 UserController.getUserById()에 연결된다. 
Spring은 URL의 {id} 자리에 있는 1을 추출하고, @PathVariable이 붙은 long id 매개변수에 넣는다. Controller는 HTTP 요청을 받아 UserService.getUserById(1)을 호출한다. Spring Data JPA가 생성한 Repository 구현체가 Hibernate를 이용해 H2 DB를 조회한다. 조회 결과가 있으면 Optional<User> 안에 User가 들어 있고, 없으면 Optional.empty()가 반환된다. 이 결과는 Repository, Service, Controller 순서로 돌아온다. 

현재 Controller에는 404처리 코드가 없으므로 사용자가 없다고 해서 명시작으로 404를 반환하도록 구현 상태는 아니다. 





# 3번 문제: DTO, Entity, 유효성 검사
다음 요청이 들어옵니다.
POST /api/users
Content-Type: application/json

{
  "name": "이민형",
  "email": "wrong-email"
}

다음 내용을 설명해보세요.

JSON은 처음에 User, UserRequest 중 어떤 객체로 변환되나요?
-> 처음에 User 객체로 변환됩낟. 
@RequestBody와 @Valid는 각각 어떤 역할을 하나요?
-> @RequestBody -> User 객체로 변환된 객체를 요청합니다. 
-> @Valid -> 

이메일 형식이 틀렸을 때 어느 코드가 이를 검사하나요?

검증이 실패해도 UserService.createUser()가 실행되나요?

UserRequest와 User를 따로 사용하는 이유는 무엇인가요?

정상 요청이라면 ID는 누가 최종 생성하나요?

이번에는 특히 다음 표현을 구분해보세요.